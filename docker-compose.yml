services:
  resident-evil-tracker-client:
    image: nginx:alpine
    container_name: resident-evil-tracker-client
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
